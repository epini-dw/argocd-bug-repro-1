# This App will create a ConfigMap of the same name under the "test" namespace.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: scenario-1-one
  namespace: argocd

spec:
  project: scenario-1
  destination:
    server: https://kubernetes.default.svc
    namespace: test
  sourceHydrator:
    drySource:
      path: charts/example
      repoURL: git://git-daemon/
      targetRevision: main
      helm:
        releaseName: 'one'
        valuesObject:
          namespace: test
          configmap:
            name: 'scenario-1-one'
            data:
              message: Hello world!
    syncSource:
      path: 'manifests/scenario-1/one'
      targetBranch: hydrated
  syncPolicy:
    automated:
      enabled: true
