---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configmap.name | required "configmap.name required" }}
  namespace: {{ .Values.namespace | default .Release.Namespace }}
data:
  {{ .Values.configmap.data | toYaml | nindent 2 }}
